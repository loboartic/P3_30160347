<div class="border rounded-md bg-neutral-50 shadow-md grid grid-cols-12 hover:-translate-y-3 transition ease-in delay-150" data-id="<%= product.id %>">
  <div class="flex flex-col p-4 col-span-12">
    <p class="text-gray-950 text-3xl pb-2 font-bold"><%= product.name %></p>
    <p class="text-gray-600 font-bold rounded border p-2 text-lg">$<%= product.price %></p>
    <p class="text-sm text-gray-700">Código: <span class="text-orange-700 font-bold"><%=product.code %></span></p>
    <div class="flex flex-col">
      <button id="viewProduct" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded border border-blue-600 shadow">Ver producto</button>
      <button id="deleteProduct" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 mt-2 rounded border border-red-700 shadow">Eliminar</button>
    </div>
  </div>
</div>